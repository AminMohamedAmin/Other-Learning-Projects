from django.db import models

# Create your models here.

#note
#  __str__ == __unicode__

#field attribute note
# date=......(auto_now=False) >> put tht current date
# date=......(auto_now=True) >> put tht date by yourself